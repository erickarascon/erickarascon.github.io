---
title: "Publications"
permalink: /publications/
layout: single
author_profile: true
---

{%- comment -%}
  This page renders every publication from _data/publications.yml,
  bucketed by type (journal articles, then book chapters).
  To add a paper, edit publications.yml — do not edit this file.
{%- endcomment -%}

<div class="pub-list">

  {%- assign journal_articles = site.data.publications | where: "type", "journal" -%}
  {%- if journal_articles.size > 0 %}
  <p class="pub-section-heading">Journal articles</p>
  {% for pub in journal_articles %}
    {% include publication-entry.html pub=pub %}
  {% endfor %}
  {% endif %}

  {%- assign book_chapters = site.data.publications | where: "type", "book_chapter" -%}
  {%- if book_chapters.size > 0 %}
  <p class="pub-section-heading">Book chapters</p>
  {% for pub in book_chapters %}
    {% include publication-entry.html pub=pub %}
  {% endfor %}
  {% endif %}

</div>
